server:
  port: 8081
  servlet:
    context-path: /api/v1

spring:
  application:
    name: bank-service
  jta:
    enabled: true
    transaction-manager-id: bank-tm
    atomikos:
      properties:
        transaction-manager-unique-name: ${BANK_ATOMIKOS_TM_UNIQUE_NAME:bank-service-tm}
        log-base-dir: ${BANK_ATOMIKOS_LOG_DIR:./data/tx-logs/bank}
        log-base-name: ${BANK_ATOMIKOS_LOG_BASE_NAME:tmlog-bank}
  datasource:
    xa:
      data-source-class-name: org.postgresql.xa.PGXADataSource
      properties:
        serverName: ${BANK_DB_HOST:${TICKET_DB_HOST:localhost}}
        portNumber: ${BANK_DB_PORT:5432}
        databaseName: ${BANK_DB_NAME:${TICKET_DB_NAME:postgres}}
        user: ${BANK_DB_USER:${TICKET_DB_USER:postgres}}
        password: ${BANK_DB_PASSWORD:${TICKET_DB_PASSWORD:postgres}}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        transaction:
          coordinator_class: jta
      jakarta:
        persistence:
          transactionType: JTA

security:
  users-xml: classpath:security/users.xml

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:*}
  allowed-methods: ${CORS_ALLOWED_METHODS:GET,POST,PUT,PATCH,DELETE,OPTIONS}
  allowed-headers: ${CORS_ALLOWED_HEADERS:*}
  exposed-headers: ${CORS_EXPOSED_HEADERS:Location}
  allow-credentials: ${CORS_ALLOW_CREDENTIALS:false}
  max-age: ${CORS_MAX_AGE:3600}
